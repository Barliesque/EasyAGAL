package com.barliesque.agal {
	
	/**
	 * Marks a class as a datatype that may be passed as an opcode parameter
	 * @usage  Use this interface when assigning variables as "aliases" to registers or register component selections, or when defining parameters for a macro function.
	 * @author David Barlia
	 */
	public interface IField { }
	
}