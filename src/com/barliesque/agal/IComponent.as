package com.barliesque.agal {
	
	/**
	 * Marks a class as representing a single AGAL component
	 * @usage  Use this interface when assigning variables as "aliases" to register components, or when defining parameters for a macro function.
	 * @author David Barlia
	 */
	public interface IComponent extends IField { }
	
}