package com.barliesque.agal {
	
	/**
	 * Marks a register class as being a SAMPLER register - which does not support component selection
	 * @usage  Use this interface when assigning variables as "aliases" to texture sampler registers, or when defining parameters for a macro function.
	 * @author David Barlia
	 */
	public interface ISampler { }
	
}